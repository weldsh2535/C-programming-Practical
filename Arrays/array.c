#include <stdio.h>
#include <string.h>

int main(){
    int i,k,j,numbers,b[90];
    int a[5] = {3,5,1,8,9};
    //printf("Array of %d %d %d %d %d\n",a[0],a[1],a[2],a[3],a[4]); 
    for (int i = 0; i < 5; i++)
    {
        printf("Array of %d is %d \n",i , a[i]);
    }
   
    return 0;
}